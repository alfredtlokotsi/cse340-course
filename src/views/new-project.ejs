<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>
    
    <div class="form-container">
        <p class="form-instructions">Fill out the form below to add a new service project.</p>
        
        <form action="/new-project" method="POST" class="project-form" novalidate>
            <div class="form-group">
                <label for="organizationId">Organization <span class="required">*</span></label>
                <select id="organizationId" name="organizationId" required>
                    <option value="">-- Select an Organization --</option>
                    <% if (organizations && organizations.length > 0) { %>
                        <% organizations.forEach(org => { %>
                            <option value="<%= org.organization_id %>">
                                <%= org.name %>
                            </option>
                        <% }); %>
                    <% } else { %>
                        <option value="" disabled>No organizations available</option>
                    <% } %>
                </select>
                <span class="field-note">Select the organization sponsoring this project.</span>
                <div class="error-message" id="orgError"></div>
            </div>

            <div class="form-group">
                <label for="title">Project Title <span class="required">*</span></label>
                <input
                    type="text"
                    id="title"
                    name="title"
                    placeholder="e.g., Community Park Cleanup"
                    required
                    maxlength="200"
                />
                <span class="field-note">Must be between 3 and 200 characters.</span>
                <div class="error-message" id="titleError"></div>
            </div>

            <div class="form-group">
                <label for="description">Description <span class="required">*</span></label>
                <textarea
                    id="description"
                    name="description"
                    rows="4"
                    placeholder="Describe the project, its goals, and what volunteers will do"
                    required
                    maxlength="1000"
                ></textarea>
                <span class="field-note">Maximum 1000 characters. <span id="charCount">0</span>/1000</span>
                <div class="error-message" id="descriptionError"></div>
            </div>

            <div class="form-group">
                <label for="location">Location <span class="required">*</span></label>
                <input
                    type="text"
                    id="location"
                    name="location"
                    placeholder="e.g., Central Park, 123 Main St"
                    required
                    maxlength="200"
                />
                <span class="field-note">Address or description of where the project will take place.</span>
                <div class="error-message" id="locationError"></div>
            </div>

            <div class="form-group">
                <label for="date">Project Date <span class="required">*</span></label>
                <input
                    type="date"
                    id="date"
                    name="date"
                    required
                    min="<%= new Date().toISOString().split('T')[0] %>"
                />
                <span class="field-note">Date must be today or in the future.</span>
                <div class="error-message" id="dateError"></div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-plus-circle"></i> Create Project
                </button>
                <a href="/projects" class="btn btn-secondary">
                    <i class="fas fa-times"></i> Cancel
                </a>
            </div>
        </form>
        
        <div class="form-footer">
            <p>All fields marked with <span class="required">*</span> are required.</p>
            <p>Projects will be listed on the main service projects page with "Upcoming" status.</p>
        </div>
    </div>
</main>

<%- include('partials/footer') %>